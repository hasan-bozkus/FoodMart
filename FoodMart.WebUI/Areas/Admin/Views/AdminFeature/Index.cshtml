@model List<ResultFeatureDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayout/_AdminLayout.cshtml";
}


<div class="card">
    <h5 class="card-header">Öne Çıkan Paneli</h5>
    <div class="table-responsive text-nowrap">
        <table class="table">
            <thead>
                <tr>
                    <th>Başlık</th>
                    <th>Alt Başlık</th>
                    <th>Güncelle</th>
                    <th>Sil</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                @foreach (var item in Model)
                {
                    <tr>
                        <td><i class="fab fa-angular fa-lg text-danger me-3"></i> <strong>@item.Title</strong></td>
                        <td><i class="fab fa-angular fa-lg text-danger me-3"></i> <strong>@item.SubTitle</strong></td>
                        <td>
                            <a class="btn btn-outline-success" href="/Admin/AdminFeature/UpdateFeature/@item.FeatureId"><i class="bx bx-edit-alt me-1"></i> Güncelle</a>
                        </td>
                        <td>
                            <a class="btn btn-outline-danger" href="/Admin/AdminFeature/DeleteFeature/@item.FeatureId"><i class="bx bx-trash me-1"></i> Sil</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <br />
        <a href="/Admin/AdminFeature/CreateFeature" class="btn btn-primary">Yeni Öne Çıkan Girişi</a>
        <br />
        <br />

    </div>
</div>

